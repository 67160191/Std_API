# บันทึกผลการทดสอบ Attack Scenario — wk04-lab

## ขั้นตอนที่ 3.4 จำลอง Attack Scenario พื้นฐาน: Payload ขนาดใหญ่ผิดปกติ (Large Payload)

### วิธีทดสอบ
ทดสอบการจำกัดขนาด body ด้วย `express.json({ limit: "10kb" })` โดยส่งคำขอ POST `/api/v1/students` พร้อม body ที่มีขนาดเกิน 10KB 

ตั้งค่าใน Postman:
1. ไปที่แท็บ **Scripts** (หรือ Pre-request Script) ใส่โค้ดเพื่อสร้างตัวแปร string ยาว 15,000 ตัวอักษร (ประมาณ 15KB):
   ```javascript
   pm.variables.set("longName", "x".repeat(15000));
   ```
2. ไปที่แท็บ **Body** เลือก raw และประเภท JSON ใส่ข้อมูลโดยเรียกใช้ตัวแปร `{{longName}}`:
   ```json
   {
     "name": "{{longName}}",
     "major": "วิทยาการคอมพิวเตอร์",
     "email": "test@example.com"
   }
   ```

### ผลที่ได้
- **Status Code**: `413 Payload Too Large`
- **Response Body**: เซิร์ฟเวอร์จะตอบกลับด้วยโครงสร้าง Error มาตรฐาน พร้อมข้อความระบุว่า `request entity too large` หรือ `Payload Too Large` (ขึ้นอยู่กับ Error Handler)

### ข้อสังเกตของตนเอง
- Middleware `express.json({ limit: "10kb" })` ทำงานได้ถูกต้อง โดยจะบล็อก Request ที่มีขนาดใหญ่กว่าที่กำหนดไว้ทันทีก่อนที่จะส่งต่อไปยัง Route Handler
- ข้อควรระวังคือ ต้องแน่ใจว่าส่ง Request ด้วย Header `Content-Type: application/json` เท่านั้น Middleware ตัวนี้จึงจะทำงาน
- การกำหนดข้อจำกัดนี้มีความสำคัญมากในแง่ของความปลอดภัย เพราะช่วยป้องกันการโจมตีแบบ Denial of Service (DoS) ที่ผู้ไม่หวังดีอาจพยายามส่งข้อมูลขนาดใหญ่มากๆ เพื่อทำให้เซิร์ฟเวอร์หน่วยความจำเต็มหรือทำงานหนักจนล่มได้

---

## ขั้นตอนที่ 3.5 จำลอง Attack Scenario พื้นฐาน: การส่งข้อมูลประเภทผิด (Type Confusion)

### วิธีทดสอบ
ส่งคำขอ POST `/api/v1/students` โดยตั้งใจระบุฟิลด์ `name` เป็น Object แทนที่จะเป็น String แบบปกติ:

```json
{
  "name": { "malicious": "payload" },
  "major": "วิทยาการคอมพิวเตอร์",
  "email": "test2@example.com"
}
```

### ผลที่ได้
- **Status Code**: `201 Created`
- **พฤติกรรมที่เกิดขึ้น**: ระบบสามารถบันทึกข้อมูลได้สำเร็จ และในฐานข้อมูลจำลอง (หรือเมื่อ Get ข้อมูลดู) ฟิลด์ name จะถูกเก็บเป็น Object หรือเมื่อถูกแสดงผลอาจกลายเป็นค่าแปลกๆ (เช่น `[object Object]`) 

### ข้อสังเกตของตนเอง
- โค้ดใน Route ปัจจุบันตรวจสอบเพียงว่าฟิลด์มีค่าหรือไม่ (เช็คแค่ `!name` ซึ่งถือเป็น truthy แม้ว่าจะเป็น Object หรือ Array) แต่ **ไม่ได้ตรวจสอบชนิดข้อมูล (Type Validation)** 
- การปล่อยให้ข้อมูลประเภทที่ไม่คาดคิดหลุดเข้าไปในระบบ ถือเป็นช่องโหว่ประเภท **Injection (A05 ตาม OWASP Top 10:2025)** ซึ่งอาจนำไปสู่ปัญหาลุกลามต่อเนื่อง (เช่น NoSQL Injection หากใช้คู่กับ MongoDB หรือทำให้แอปพลิเคชันฝั่ง Frontend แครชเมื่อพยายามนำ string method ไปใช้กับ Object)
- **วิธีแก้ไข**: แม้ Middleware พื้นฐานอย่าง Helmet จะช่วยป้องกันได้ระดับหนึ่ง แต่ความปลอดภัยระดับข้อมูล (Data Integrity) จำเป็นต้องมีการตรวจสอบที่รัดกุมกว่านี้ในชั้น Business Logic ซึ่งในสัปดาห์ที่ 13 เราจะนำไลบรารีอย่าง Zod มาใช้เพื่อบังคับชนิดข้อมูลอย่างเป็นระบบครับ
